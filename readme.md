# C语言宏(macro)展开算法实现

代码仅实现了基本的C语言宏展开原理，并未实现一个可用的宏预处理器。

步骤：
1. 解析 #define 指令（对象式宏 / 函数式宏）
2. 对源代码中的宏调用进行展开（替换）
3. 支持 #  字符串化（stringizing）
4. 支持 ## 标记粘贴（token pasting）
5. 通过"隐藏集"（hide set / 扩展规则）防止无限递归
6. 替换体压回栈后自动重新扫描（rescan）
